{
  "name": "beautyfile-addon",
  "private": true,
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "deploy:test": "node scripts/deploy-test.mjs",
    "open": "clasp open",
    "logs": "clasp logs",
    "clasp:status": "clasp status",
    "test": "vitest run",
    "test:watch": "vitest",
    "fonts:sync": "node scripts/sync-fonts.mjs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "vitest": "^3.2.4"
  }
}
