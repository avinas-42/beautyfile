# Copier vers .env si votre environnement charge ce fichier, ou exporter dans le shell.
# Ne jamais committer .env (voir .gitignore).

# Snapshot officiel Google Fonts (optionnel) — npm run fonts:sync
# GOOGLE_FONTS_API_KEY=

# Déploiement test Marketplace — npm run deploy:test
# BEAUTYFILE_DEPLOYMENT_ID=
